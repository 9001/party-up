plugins {
    alias(libs.plugins.android.application)
}

android {
    namespace "me.ocv.partyup"
    compileSdk 36

    defaultConfig {
        applicationId "me.ocv.partyup"
        minSdk 21
        targetSdk 35
        versionCode 10901
        versionName "1.9.1"
        multiDexEnabled true
    }

    signingConfigs {
        release {
            storeFile       file(System.getenv("RELEASE_STR_FILE"))
            storePassword   System.getenv("RELEASE_STR_PASS")
            keyAlias        System.getenv("RELEASE_KEY_ALIS")
            keyPassword     System.getenv("RELEASE_KEY_PASS")
        }
    }

    buildTypes {
        release {
            minifyEnabled true
            signingConfig signingConfigs.release
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    buildFeatures {
        viewBinding true
        buildConfig true
    }

    lint {
        checkReleaseBuilds false
    }

    // "Lint found fatal errors while assembling a release target:"
    // "Google Play requires that apps target API level 29 or higher"
    // lol how about no
}

dependencies {
    implementation libs.material
    implementation libs.preference
    implementation libs.zixing.core
}
